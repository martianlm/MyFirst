<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.funtl.mpper.TBPermissionMapper">
  <select id="selectByUserId" parameterType="java.lang.Long" resultType="com.funtl.model.TBPermission">
  	SELECT
		p.*
	FROM
		tb_user_role ur
	LEFT JOIN tb_role r ON r.id = ur.role_id
	LEFT JOIN tb_role_permission rp ON r.id = rp.role_id
	LEFT JOIN tb_permission p ON p.id = rp.permission_id
	WHERE
		ur.user_id = #{userId}
  </select>
</mapper>